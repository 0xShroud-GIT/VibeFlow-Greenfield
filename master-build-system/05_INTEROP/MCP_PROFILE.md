# MCP Profile

Target MCP protocol revision `2026-07-28` with the official TypeScript SDK v2 stable line.

VibeFlow uses MCP for tool/resource/prompt interfaces and may expose its own MCP server to external assistants. VibeFlow Connection/Grant/Approval policy wraps MCP calls; discovered tools are never automatically authorized.
