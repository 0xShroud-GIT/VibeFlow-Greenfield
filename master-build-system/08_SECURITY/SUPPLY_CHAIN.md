# Supply Chain

CI baseline: lockfile integrity, dependency allow/deny registry, OSV scan, secret scan, static security scan, container/SBOM scan, signed build provenance/attestation where available, protected branches, required checks. New high-risk dependencies require ADR/license/security review.
