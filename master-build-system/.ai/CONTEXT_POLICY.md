# AI Context Policy

Do not recursively load this entire repository into a coding model. Use `.ai/INDEX.yaml` and the mission row to assemble only relevant truth. Research evidence under `99_REFERENCE` is excluded unless the mission is explicitly architecture/research work.
