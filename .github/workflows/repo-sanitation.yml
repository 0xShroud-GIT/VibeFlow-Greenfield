name: Repository Sanitation

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  sanitize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check repository hygiene
        shell: bash
        run: bash scripts/repo-sanitize.sh
